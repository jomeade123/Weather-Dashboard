{
  "name": "server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "npx tsc",
    "start": "npm run build && node dist/server.js",
    "dev": "npx nodemon",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}